services:
  - type: web
    name: house-backend
    plan: free
    env: docker
    rootDir: backend
    startCommand: gunicorn -w 4 -k uvicorn.workers.UvicornWorker src.app:app --bind 0.0.0.0:10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
